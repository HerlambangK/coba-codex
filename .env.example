# App
NUXT_APP_NAME=Nuxt Starter
NUXT_PUBLIC_BASE_URL=http://localhost:3000

# Auth
JWT_SECRET=please_change_me
JWT_EXPIRES_IN=7d
VERIFICATION_CODE_TTL_MINUTES=15

# Database
# For local (non-Docker) runs, connect to Postgres on localhost.
# When using Docker Compose, the app container overrides DATABASE_URL to
# postgresql://postgres:postgres@db:5432/appdb?schema=public
DATABASE_URL=postgresql://herlambangk:masuk123@localhost:5432/dbname?schema=public

# SMTP (Mailtrap or local SMTP server)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=ccoba8844@gmail.com
SMTP_PASS=coba8844
SMTP_FROM="Nuxt Starter <no-reply@example.com>"

MAIL_LOG=true
# SMTP_SECURE=true # uncomment if your SMTP requires SSL on port 465
# SMTP_TLS_INSECURE=true # uncomment to disable TLS cert verification for local testing

MAIL_USE_ETHEREAL=true
